# Production — copy to Vercel Environment Variables or .env.production.local before `npm run build`
# Admin API is served by Laravel at api.wnskcex.com (routes prefix: /api/admin)

NEXT_PUBLIC_ADMIN_API_URL=https://api.wnskcex.com/api/admin
